This folder contains minimal compatibility implementations of a few Image Processing Toolbox functions
(padarray, mat2gray, im2double, im2uint8) used by filters.

The GUI adds this folder to the MATLAB path only if the real functions are missing.
